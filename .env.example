# Database
# SQLite (default): sqlite:///./watch.db
# Postgres:         postgresql+psycopg://USER:PASS@HOST:PORT/DBNAME
DATABASE_URL=sqlite:///./watch.db

# Reddit (create a "script" app at https://www.reddit.com/prefs/apps)
REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT=watcher-backend/0.1 by u/your_username

# Email (Gmail SMTP via App Password)
GMAIL_FROM=your_email@gmail.com
GMAIL_APP_PASSWORD=your_app_password_here

# Polling behavior
FETCH_LIMIT=100
MAX_RETRIES=3
RETRY_DELAY=5
POLL_INTERVAL=900

# Authentication (REQUIRED for production)
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=generate_a_secure_random_key

# Token expiration in minutes (default: 1440 = 24 hours)
JWT_EXPIRATION_MINUTES=1440

# CORS origins (comma-separated, for production use your domain)
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Password reset base URL (use your production domain)
PASSWORD_RESET_BASE_URL=http://localhost:8000
